# ─── Frontend (Next.js dashboard) environment ──────────────────────────────
# Copy to .env.local for local dev, OR set these in the Vercel project
# Settings → Environment Variables for the hosted deploy.

# Supabase Postgres connection — MUST use the Transaction Pooler host
# (e.g. aws-0-<region>.pooler.supabase.com:6543). The direct db.<ref>...
# host is IPv6-only and times out from Vercel.
DATABASE_URL=postgresql://USER:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres

# ─── Interim access gate (see middleware.ts) ───────────────────────────────
# DASHBOARD_PASSWORD : the shared password HR types on /login
# DASHBOARD_AUTH_TOKEN: a long random secret — the value stored in the
#                       auth cookie. Generate once, keep stable. Example:
#                       node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# If DASHBOARD_AUTH_TOKEN is UNSET, the gate is disabled (handy for local
# dev). ALWAYS set both in the Vercel project so the hosted site is gated.
DASHBOARD_PASSWORD=change-me-to-a-team-password
DASHBOARD_AUTH_TOKEN=REPLACE_WITH_64_CHAR_RANDOM_HEX

# ─── Supabase Storage (used by /policies file library) ────────────────────
# Required for the /policies page to list & open uploaded HR policy PDFs.
# Get these from Supabase Dashboard → Settings → API.
#   SUPABASE_URL                : project URL, e.g. https://<ref>.supabase.co
#   SUPABASE_SERVICE_ROLE_KEY   : service_role JWT (the SECRET one, not anon).
#                                 server-side only — never exposed to the browser.
# The /policies page lists & opens files using public Storage URLs, so reads
# work even without these.  Upload/edit/delete routes (currently disabled in
# the UI but live in the backend) need both vars set.
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_SERVICE_ROLE_JWT

# ─── RUH HRMS identity integration (PRD 09, Path B) ────────────────────────
# Shlok's time-tracker is the identity provider. We VERIFY his RS256 JWTs
# against his JWKS — we NEVER mint tokens, so there is NO shared secret and
# NO private key here. Verification (lib/jwt.ts) checks signature + iss + aud + exp.
JWKS_URL=https://TIMETRACKER_DOMAIN/api/.well-known/jwks.json
JWT_ISSUER=timetracker-api
JWT_AUDIENCE=timetracker-clients
# Cookie the shared session stores the access token in (confirm the exact name with Shlok):
SESSION_COOKIE=tt_access_token
# Auth mode switch: `legacy` (DEFAULT — the hrbot_user password gate) or `shared` (Shlok's JWT session,
# no second login). Keep legacy until JWKS_URL / SESSION_COOKIE / iss / aud are confirmed, then flip to shared.
AUTH_MODE=legacy
# Where AUTH_MODE=shared sends users with no session (Shlok's login). Empty → falls back to /session.
SHARED_LOGIN_URL=
# Shlok's directory endpoint = the source of PEOPLE for reconcile_users.py (read-only, email-only matching).
# Not wired yet → the reconciler runs a my-side preview. Fields expected: id / name / email / role / teams.
DIRECTORY_URL=https://TIMETRACKER_DOMAIN/api/directory/users
DIRECTORY_TOKEN=
